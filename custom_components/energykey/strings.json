{
  "title": "EnergyKey",
  "config": {
    "step": {
      "user": {
        "title": "Set up EnergyKey",
        "description": "Create an EnergyKey integration entry. API credentials will be added as the integration is implemented."
      }
    },
    "abort": {
      "already_configured": "EnergyKey is already configured."
    }
  }
}
